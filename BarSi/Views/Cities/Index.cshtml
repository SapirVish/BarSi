@model IEnumerable<BarSi.Models.City>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a hidden="!(@ViewBag.IsAdmin)" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a hidden="!(@ViewBag.IsAdmin)" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a hidden="!(@ViewBag.IsAdmin)" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a hidden="!(@ViewBag.IsAdmin)" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
